using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class UIManager : MonoBehaviour
{
    public TMP_Text playerPref;
    public TMP_Text jsonObject;
    public TMP_Text scriptable;
    public TMP_Text serializable;
    
    // Start is called before the first frame update
    void Start()
    {
        // playerPref.text = "Player Pref";
        // jsonObject.text = "Json";
        // scriptable.text = "Scriptable";
        // serializable.text = "Serializable";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
